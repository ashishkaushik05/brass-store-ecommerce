# Root-level environment template (legacy / reference only)
# Each sub-package has its own .env — see:
#   backend/.env.example   — MongoDB, Clerk, PhonePe, ngrok config
#   frontend/.env.example  — Vite API base URL, Clerk publishable key
#
# NEVER commit .env files with real secrets to git.
